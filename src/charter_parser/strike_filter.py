from __future__ import annotations

from charter_parser.models import PageIR


def apply_strike_filter(page: PageIR) -> PageIR:
    # Placeholder for future geometry-based strike-through removal.
    # Keep the interface now so Milestone 2 can wire vector-line logic here.
    return page
