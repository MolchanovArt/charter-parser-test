from __future__ import annotations

from charter_parser.models import CandidateBlock, PageIR


def generate_candidates(page: PageIR) -> list[CandidateBlock]:
    # Placeholder: future milestone will convert lines into new-clause / continuation / noise candidates.
    return []
